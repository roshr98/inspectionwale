version: 1
frontend:
  phases:
    preBuild:
      commands: []
    build:
      commands: []
  artifacts:
    # Publish the CarServ-based site directory so Amplify serves the carserv-template pages
    baseDirectory: website/carserv-template
    files:
      - '**/*'
  cache:
    paths: []
